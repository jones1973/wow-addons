<!-- ui/files.xml -->
<Ui>
    <!-- Shared UI primitives (drop-in from shared library) -->
    <Include file="shared\files.xml"/>

    <!-- Panel + rows + headers + scroll -->
    <Script file="panel.lua"/>

    <!-- Filter popup (anchored to panel's right edge) -->
    <Script file="filterPopup.lua"/>

    <!-- AH tab wiring -->
    <Script file="ahTab.lua"/>
</Ui>
